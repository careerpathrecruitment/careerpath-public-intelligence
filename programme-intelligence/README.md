# CareerPath Programme Intelligence public feed

This directory is generated automatically from CareerPath's validated public Programme Intelligence export.

It intentionally contains only the curated public data contract. Raw vacancies, internal bid scoring, private engine state and commercial recommendations are excluded before publication.
